/*package com.xiaolin.xoverhaul.world.gen.structures;

import net.minecraft.structure.StructureManager;
import net.minecraft.structure.StructurePiece;
import net.minecraft.util.BlockRotation;
import net.minecraft.util.Identifier;
import net.minecraft.util.math.BlockPos;

import java.util.List;

public class WoodenCrateGenerator {

    private static final Identifier WOODEN_CRATE = new Identifier("wooden_crate/wooden_crate");

    public static void addPieces(StructureManager manager, BlockPos pos, BlockRotation rotation, List<StructurePiece> pieces) {
        pieces.add(new MyPiece(manager, pos, WOODEN_CRATE, rotation));
    }

}
*/